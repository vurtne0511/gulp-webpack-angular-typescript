import { Component } from '@angular/core';
import { MENU } from '../../constants/menu';

@Component({
    selector: 'navigator',
    templateUrl: './app/core/navigator/navigator.html'
})
export default class Navigator {

    constructor() {

    }
}